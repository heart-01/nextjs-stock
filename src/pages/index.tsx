import React from "react";
import Layout from "@/components/layouts/Layout";

type Props = {};

const index = ({}: Props) => {
  return (
    <Layout>
      <div>index</div>
    </Layout>
  );
};

export default index;
